const menu=document.querySelector('.menu');const nav=document.querySelector('#mainNav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});}
const links=[...document.querySelectorAll('.side-nav a')];const sections=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){links.forEach(a=>a.classList.toggle('selected',a.getAttribute('href')==='#'+e.target.id));}})},{rootMargin:'-25% 0px -65%'});sections.forEach(s=>io.observe(s));
